import React from "react";

import styles from '../App.module.css'

const NotFoundPage = () => (
  <h2 className={styles.pageContent}>Упс... Похоже вы забрели не туда :)</h2>
);

export default NotFoundPage;
